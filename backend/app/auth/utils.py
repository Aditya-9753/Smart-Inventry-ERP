def send_reset_password_email(email_to: str, email: str, token: str):
    # Dummy implementation for now, in a real app use a library like 'emails'
    print(f"Sending password reset email to {email_to} with token {token}")
