def format_trend_data_for_recharts(trend_data):
    # Recharts expects an array of objects
    # This is already formatting in calculations but we can add more specific recharts shaping here if needed
    return trend_data
